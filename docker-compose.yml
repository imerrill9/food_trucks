version: '3.7'
services:
  db:
    image: postgres:14
    environment:
      POSTGRES_DB: food_trucks_dev
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
